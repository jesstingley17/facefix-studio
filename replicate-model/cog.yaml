build:
  gpu: true
  # Hardware recommendation: T4 GPU (16GB VRAM) - $0.81/hr
  # Sufficient for Stable Diffusion v1.5, cost-effective
  # Alternative options if needed:
  # - L40S: 48GB VRAM ($3.51/hr) - for larger models or faster inference
  # - A100: 80GB VRAM ($5.04/hr) - maximum performance
  python_version: "3.10"
  python_requirements: "requirements.txt"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
  cuda: "11.8"

image: "r8.im/jesstingley17/facefix-studio"
predict: "predict.py:Predictor"

